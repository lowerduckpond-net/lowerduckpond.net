[project]
name = "lowerduckpond-provisioner"
version = "0.0.0"
description = "Narrowly privileged provisioner for Lower Duck Pond Hosting"
requires-python = ">=3.14,<3.15"
dependencies = []

[project.scripts]
lowerduckpond-provisioner = "lowerduckpond_provisioner.cli:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13"]
build-backend = "uv_build"
