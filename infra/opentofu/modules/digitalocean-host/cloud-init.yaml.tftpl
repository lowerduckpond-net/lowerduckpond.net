#cloud-config
users:
  - default
  - name: ${admin_username}
    groups:
      - sudo
    lock_passwd: true
    shell: /bin/bash
    ssh_authorized_keys:
      - ${admin_ssh_public_key}
    sudo:
      - ALL=(ALL) NOPASSWD:ALL
ssh_pwauth: false
disable_root: true
