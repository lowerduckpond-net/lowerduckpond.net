# OpenTofu foundation

Milestone 0 contains valid, resource-free roots so formatting and validation are
real from the first change. DigitalOcean and Cloudflare providers, remote state,
and resource modules begin in Milestone 1.

Never commit state, saved production plans, backend credentials, or provider
tokens. Production changes will run through protected, serialized CI.
