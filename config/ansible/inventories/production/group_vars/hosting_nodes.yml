---
firewall_admin_source_cidrs: >-
  {{ lookup('ansible.builtin.env', 'ADMIN_SOURCE_CIDRS_JSON') |
  default('[]', true) | from_json }}
caddy_cloudflare_api_token: >-
  {{ lookup('ansible.builtin.env', 'CADDY_CLOUDFLARE_API_TOKEN') }}
caddy_origin_pull_ca_paths: >-
  {{ lookup('ansible.builtin.env', 'CADDY_ORIGIN_PULL_CA_PATHS_JSON') |
  default('[]', true) | from_json }}
caddy_origin_pull_enforcement_enabled: false
caddy_generation_enabled: true

backup_repository: "{{ lookup('ansible.builtin.env', 'BACKUP_REPOSITORY') }}"
backup_restic_password: "{{ lookup('ansible.builtin.env', 'RESTIC_PASSWORD') }}"
backup_spaces_access_key_id: >-
  {{ lookup('ansible.builtin.env', 'SPACES_BACKUP_ACCESS_KEY_ID') }}
backup_spaces_secret_access_key: >-
  {{ lookup('ansible.builtin.env', 'SPACES_BACKUP_SECRET_ACCESS_KEY') }}
backup_spaces_region: "{{ lookup('ansible.builtin.env', 'SPACES_REGION') }}"

static_host_agent_artifact_path: >-
  {{ lookup('ansible.builtin.env', 'STATIC_HOST_AGENT_ARTIFACT_PATH') }}
static_host_agent_artifact_sha256: >-
  {{ lookup('ansible.builtin.env', 'STATIC_HOST_AGENT_ARTIFACT_SHA256') }}
static_publication_enabled: false

static_operator_public_key: >-
  {{ lookup('ansible.builtin.env', 'STATIC_OPERATOR_PUBLIC_KEY') }}
static_operator_principal: >-
  {{ lookup('ansible.builtin.env', 'STATIC_OPERATOR_PRINCIPAL') }}
