# Ansible foundation

Milestone 0 supplies a valid, no-op playbook and localhost inventory so linting
and syntax checks exercise real Ansible input. Host roles begin in Milestone 2.

Production inventory and credentials must not be committed.
