---
- name: Write the exact qualification convergence marker
  ansible.builtin.copy:
    content: |
      {{ m3_expected_run_id }} {{ m3_expected_source_revision }} {{ m3_qualification_origin_pull_trust }}
    dest: "{{ m3_qualification_convergence_marker }}"
    owner: root
    group: root
    mode: "0400"
